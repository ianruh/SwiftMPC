name: Swift

on: [push]

jobs:
  build:

    runs-on: [ubuntu-latest, macos-latest]

    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: make build
      - name: Run tests
        run: make test
